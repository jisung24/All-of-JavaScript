'use strict';

// ========= alert prompt confirm ==========
/**
 * alert(경고한다.); //window객체 안에다가!
 * => id가 이게 아닙니다.
 * => pw오류!
 * => 뭘 반드시 입력해야됨. null값이 허용이 되지않음.
 * 
 */

const name = null;

if(name === null){
    alert('이름은 입력하셔야합니다!');
}else{
    
}